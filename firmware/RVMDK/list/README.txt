本目录是正式 STC8G1K08 RESET 工程的 Keil C51 输出目录。

正式构建应生成：

- `stc8g1k08_reset.hex`：现场烧录文件
- Keil 生成的同名 `.lst`、`.obj`、`.m51`、`.lnp` 等中间文件

旧 SMOKE/MONITOR 输出已归档到 `../../validation/RVMDK-list-history/`，不作为正式固件输入。
