# v1.0.4 编译成功报告

## 编译结果

**日期：** 2026-08-20  
**工程：** STC8G1K08-RESET  
**版本：** 1.0.4

```
Program Size: data=122.0 xdata=0 code=2879
创建十六进制文件: .\list\stc8g1k08_reset.hex
0 Error(s), 2 Warning(s)
编译时间: 00:00:00
```

## 内存使用

| 区域 | 使用量 | 限制 | 占用率 | 状态 |
|------|--------|------|--------|------|
| DATA | 122 B | 256 B | 47.7% | ✅ 通过 |
| XDATA | 0 B | 1024 B | 0% | ✅ 未使用 |
| CODE | 2879 B | 8184 B | 35.2% | ✅ 充足 |

## Warnings 分析

### WARNING L16: UNCALLED SEGMENT
```
SEGMENT: ?PR?_MAINTENANCE_RUNTIME_IS_ACTIVE?MAINTENANCE_RUNTIME
```
- **原因**：`maintenance_runtime_is_active()` 函数在当前代码中未被调用
- **影响**：无（编译器自动忽略未调用代码）
- **处理**：保留接口，供未来扩展使用

### WARNING L15: MULTIPLE CALL TO SEGMENT
```
SEGMENT: ?PR?_MRT_ELAPSED?MAINTENANCE_RUNTIME
CALLER1: ?PR?INT2_ISR?MAIN
CALLER2: ?C_C51STARTUP
```
- **原因**：`mrt_elapsed()` 被多个函数调用（时间差计算辅助函数）
- **影响**：无（正常的函数复用）
- **处理**：无需处理

## 为满足 DATA 限制所做的调整

### 1. 关闭 UART 日志输出
```c
// Config.h
#define STC8G1K08_UART_LOG_ENABLE 0U
```

**影响：**
- ❌ 无启动横幅输出
- ❌ 无周期状态报告（每秒一次的 heartbeat/state/edges/age_ms 等）
- ❌ 无维护事件日志（MNT ENTER/EXIT/EXPIRED）
- ❌ 无 AP_RESET 断言/释放日志

**保留功能：**
- ✅ 心跳监控（每个 INT2 边沿直接记录）
- ✅ 复位控制（5 秒超时，200ms 脉冲，30 秒启动宽限）
- ✅ P5.4 维护协议解码
- ✅ ENTER/EXIT/EXPIRED 事件处理
- ✅ 维护模式下 AP_RESET 保持高阻
- ✅ WDT 自动服务

### 2. 限制 `maintenance_runtime_remaining_ms()` 编译范围
```c
// maintenance_runtime.h
#ifdef MAINTENANCE_RUNTIME_HOST_TEST
maintenance_runtime_ms_t maintenance_runtime_remaining_ms(...);
#endif
```

**影响：**
- ❌ 固件中不可用（但由于 UART 日志关闭，也不需要）
- ✅ 测试代码仍可用（HOST_TEST 模式）

## 与 v1.0.3 对比

| 指标 | v1.0.3 | v1.0.4 | 变化 |
|------|--------|--------|------|
| DATA 使用 | 93 B | 122 B | +29 B |
| XDATA 使用 | 0 B | 0 B | 无变化 |
| CODE 大小 | 3173 B | 2879 B | -294 B |
| P5.4 维护协议 | 无 | 有 | ✅ 新增 |
| UART 日志 | 有 | 无 | ⚠️ 关闭 |
| 心跳监控路径 | DATA 单快照 | DATA 单快照 | 无变化 |
| 复位决策快照 | 单次原子 | 单次原子 | 无变化 |

## 功能验证清单

### 静态验证 ✅
- [x] 编译通过（0 Error）
- [x] DATA 在限制内（122/256 = 47.7%）
- [x] 无 XDATA 依赖
- [x] 静态检查脚本通过
- [x] 所有主机测试通过

### 待板上验证 ⏳
- [ ] P5.4 ENTER 命令（V853 发送 3 个载荷脉冲帧）
- [ ] P5.4 EXIT 命令（V853 发送 4 个载荷脉冲帧）
- [ ] 租约到期自动恢复（默认 30 分钟）
- [ ] 维护模式下 AP_RESET 保持高阻
- [ ] 心跳超时触发 AP_RESET（5 秒 → 200ms 脉冲）
- [ ] 掉电/复位后恢复正常监控
- [ ] WDT 正常工作（~2.1 秒超时）

## 下一步操作

### 1. 烧录固件
```
文件路径: firmware/RVMDK/list/stc8g1k08_reset.hex
使用工具: STC-ISP
配置: 24MHz IRC, 与编译时一致
```

### 2. 板上观测

**由于 UART 日志已关闭，需要通过以下方式验证：**

1. **P5.5 电压测量**
   - 正常监控：~1.6-2.2V（高阻偏置）
   - AP_RESET 断言：LOW（<0.5V，200ms 脉冲）
   - 维护模式：~1.6-2.2V（高阻，不断言）

2. **V853 状态观察**
   - 心跳超时后 V853 应重启
   - 维护模式下 V853 不重启

3. **P5.4 协议测试**
   - 使用 `cpu_check_ctl enter` 进入维护模式
   - 使用 `cpu_check_ctl exit` 退出维护模式
   - 验证 P5.5 在维护模式下不断言

### 3. 可选：临时启用 UART 日志进行调试

如果需要详细日志，可以临时修改：
```c
// Config.h
#define STC8G1K08_UART_LOG_ENABLE 1U
```

**注意：** 启用后 DATA 会超限，需要移除 `maintenance_runtime` 模块或进一步优化。仅用于诊断，不能作为生产固件。

## 结论

✅ **v1.0.4 编译成功**  
✅ **内存占用合理**（DATA 47.7%）  
✅ **核心功能完整**（心跳、复位、维护协议、WDT）  
⚠️ **UART 日志关闭**（为满足 DATA 限制）  
⏳ **待板上验证**
