C51 COMPILER V9.54   MAINTENANCE_RUNTIME                                                   08/21/2026 10:55:33 PAGE 1   


C51 COMPILER V9.54, COMPILATION OF MODULE MAINTENANCE_RUNTIME
OBJECT MODULE PLACED IN .\list\maintenance_runtime.obj
COMPILER INVOKED BY: C:\Keil_v5\C51\BIN\C51.EXE ..\Driver\src\maintenance_runtime.c OPTIMIZE(8,SPEED) BROWSE INCDIR(..\U
                    -ser;..\Driver\inc) DEBUG OBJECTEXTEND PRINT(.\list\maintenance_runtime.lst) TABS(2) OBJECT(.\list\maintenance_runtime.ob
                    -j)

line level    source

   1          #include "maintenance_runtime.h"
   2          
   3          #ifdef MAINTENANCE_RUNTIME_HOST_TEST
              typedef maintenance_runtime_ms_t mrt_interval_t;
              #define MRT_EDGE_ELAPSED(now_ms, then_ms) \
                  ((maintenance_runtime_ms_t)((now_ms) - (then_ms)))
              #define MRT_LEASE_DEADLINE(now_ms) \
                  ((maintenance_runtime_ms_t)((now_ms) + MRT_DEFAULT_LEASE_MS))
              #define MRT_LEASE_EXPIRED(mrt, now_ms) \
                  ((maintenance_runtime_ms_t)((now_ms) - (mrt)->lease_end_ms) < 0x80000000UL)
              #else
  12          typedef unsigned int mrt_interval_t;
  13          #define MRT_LEASE_TICK_SHIFT 8U
  14          #define MRT_LEASE_TICK_ROUND 255UL
  15          #define MRT_EDGE_ELAPSED(now_ms, then_ms) \
  16              ((unsigned int)((unsigned int)(now_ms) - (unsigned int)(then_ms)))
  17          #define MRT_LEASE_DEADLINE(now_ms) \
  18              ((unsigned int)((unsigned int)((now_ms) >> MRT_LEASE_TICK_SHIFT) + \
  19                              (unsigned int)((MRT_DEFAULT_LEASE_MS + MRT_LEASE_TICK_ROUND) >> \
  20                                             MRT_LEASE_TICK_SHIFT)))
  21          #define MRT_LEASE_EXPIRED(mrt, now_ms) \
  22              ((unsigned int)((unsigned int)((now_ms) >> MRT_LEASE_TICK_SHIFT) - \
  23                              (mrt)->lease_end_ms) < 0x8000U)
  24          #endif
  25          
  26          /* 重置解码器状态（保留 have_edge 和 maintenance_active）*/
  27          static void mrt_decoder_reset(maintenance_runtime_t *mrt)
  28          {
  29   1          unsigned char state = mrt->state;
  30   1          MRT_SET_DECODER_STATE(state, MRT_DECODER_IDLE);
  31   1          MRT_SET_PAYLOAD_COUNT(state, 0U);
  32   1          mrt->state = state;
  33   1          /* have_edge 和 maintenance_active 保持不变 */
  34   1      }
  35          
  36          /* 初始化 */
  37          void maintenance_runtime_init(maintenance_runtime_t *mrt,
  38                                        maintenance_runtime_ms_t now_ms)
  39          {
  40   1          if (mrt == 0) return;
  41   1          mrt->last_edge_ms = now_ms;
  42   1          mrt->lease_end_ms = 0UL;
  43   1          mrt->state = 0U;  /* 全部字段初始化为 0 */
  44   1      }
  45          
  46          /* INT2 边沿处理 */
  47          void maintenance_runtime_on_falling_edge(maintenance_runtime_t *mrt,
  48                                                   maintenance_runtime_ms_t now_ms)
  49          {
  50   1          mrt_interval_t interval;
  51   1          unsigned char state;
  52   1          unsigned char decoder_state;
  53   1          unsigned char payload_count;
C51 COMPILER V9.54   MAINTENANCE_RUNTIME                                                   08/21/2026 10:55:33 PAGE 2   

  54   1      
  55   1          if (mrt == 0) return;
  56   1      
  57   1          state = mrt->state;
  58   1      
  59   1          /* 第一个边沿 */
  60   1          if (MRT_HAVE_EDGE(state) == 0U) {
  61   2              mrt->last_edge_ms = now_ms;
  62   2              MRT_SET_HAVE_EDGE(state, 1U);
  63   2              mrt->state = state;
  64   2              return;
  65   2          }
  66   1      
  67   1          /* 计算间隔 */
  68   1          interval = MRT_EDGE_ELAPSED(now_ms, mrt->last_edge_ms);
  69   1          mrt->last_edge_ms = now_ms;
  70   1      
  71   1          decoder_state = MRT_DECODER_STATE(state);
  72   1      
  73   1          /* IDLE: 等待 BREAK */
  74   1          if (decoder_state == MRT_DECODER_IDLE) {
  75   2              if (interval >= MRT_BREAK_MIN_MS) {
  76   3                  MRT_SET_DECODER_STATE(state, MRT_DECODER_SYNC);
  77   3                  mrt->state = state;
  78   3              }
  79   2              return;
  80   2          }
  81   1      
  82   1          /* SYNC: 等待短脉冲 */
  83   1          if (decoder_state == MRT_DECODER_SYNC) {
  84   2              if (interval < MRT_SHORT_MIN_MS || interval > MRT_SHORT_MAX_MS) {
  85   3                  mrt_decoder_reset(mrt);
  86   3              } else {
  87   3                  MRT_SET_DECODER_STATE(state, MRT_DECODER_DELIMITER);
  88   3                  mrt->state = state;
  89   3              }
  90   2              return;
  91   2          }
  92   1      
  93   1          /* DELIMITER: 等待分隔符 */
  94   1          if (decoder_state == MRT_DECODER_DELIMITER) {
  95   2              if (interval < MRT_DELIMITER_MIN_MS || interval > MRT_DELIMITER_MAX_MS) {
  96   3                  mrt_decoder_reset(mrt);
  97   3              } else {
  98   3                  MRT_SET_DECODER_STATE(state, MRT_DECODER_PAYLOAD);
  99   3                  MRT_SET_PAYLOAD_COUNT(state, 0U);
 100   3                  mrt->state = state;
 101   3              }
 102   2              return;
 103   2          }
 104   1      
 105   1          /* PAYLOAD: 接收载荷脉冲 */
 106   1          payload_count = MRT_PAYLOAD_COUNT(state);
 107   1          if (interval >= MRT_TRAILER_MIN_MS) {
 108   2              /*
 109   2               * 主循环可能正被 UART 日志阻塞，导致 trailer 到期后的第一个
 110   2               * 普通心跳下降沿先进入 ISR。此时先锁存合法事件，不能把它当作
 111   2               * 错误载荷清掉；主循环稍后通过 poll() 取走事件。
 112   2               */
 113   2              if (payload_count == 3U) {
 114   3                  MRT_SET_MAINTENANCE_ACTIVE(state, 1U);
 115   3                  MRT_SET_EVENT_PENDING(state, 1U);
C51 COMPILER V9.54   MAINTENANCE_RUNTIME                                                   08/21/2026 10:55:33 PAGE 3   

 116   3                  mrt->state = state;
 117   3                  mrt_decoder_reset(mrt);
 118   3                  return;
 119   3              }
 120   2              if (payload_count == 4U) {
 121   3                  MRT_SET_MAINTENANCE_ACTIVE(state, 0U);
 122   3                  MRT_SET_EVENT_PENDING(state, 1U);
 123   3                  mrt->state = state;
 124   3                  mrt->lease_end_ms = 0UL;
 125   3                  mrt_decoder_reset(mrt);
 126   3                  return;
 127   3              }
 128   2              mrt_decoder_reset(mrt);
 129   2          } else if (interval < MRT_SHORT_MIN_MS || interval > MRT_SHORT_MAX_MS) {
 130   2              mrt_decoder_reset(mrt);
 131   2          } else {
 132   2              ++payload_count;
 133   2              if (payload_count > 4U) {
 134   3                  mrt_decoder_reset(mrt);
 135   3              } else {
 136   3                  MRT_SET_PAYLOAD_COUNT(state, payload_count);
 137   3                  mrt->state = state;
 138   3              }
 139   2          }
 140   1      }
 141          
 142          /* 轮询事件 */
 143          maintenance_runtime_event_t maintenance_runtime_poll(
 144              maintenance_runtime_t *mrt,
 145              maintenance_runtime_ms_t now_ms)
 146          {
 147   1          mrt_interval_t elapsed;
 148   1          unsigned char state;
 149   1          unsigned char decoder_state;
 150   1          unsigned char payload_count;
 151   1          unsigned char maintenance_active;
 152   1      
 153   1          if (mrt == 0) return MRT_EVENT_NONE;
 154   1      
 155   1          state = mrt->state;
 156   1          maintenance_active = MRT_MAINTENANCE_ACTIVE(state);
 157   1      
 158   1          if (MRT_EVENT_PENDING(state) != 0U) {
 159   2              MRT_SET_EVENT_PENDING(state, 0U);
 160   2              mrt->state = state;
 161   2              if (maintenance_active != 0U) {
 162   3                  mrt->lease_end_ms = MRT_LEASE_DEADLINE(now_ms);
 163   3                  return MRT_EVENT_ENTER;
 164   3              }
 165   2              return MRT_EVENT_EXIT;
 166   2          }
 167   1      
 168   1          /* 检查维护租约到期 */
 169   1          if (maintenance_active != 0U) {
 170   2              if (MRT_LEASE_EXPIRED(mrt, now_ms) != 0U) {
 171   3                  /* 租约到期，返回 NORMAL */
 172   3                  MRT_SET_MAINTENANCE_ACTIVE(state, 0U);
 173   3                  mrt->state = state;
 174   3                  mrt->lease_end_ms = 0UL;
 175   3                  return MRT_EVENT_EXPIRED;
 176   3              }
 177   2          }
C51 COMPILER V9.54   MAINTENANCE_RUNTIME                                                   08/21/2026 10:55:33 PAGE 4   

 178   1      
 179   1          /* 检查解码器状态 */
 180   1          if (MRT_HAVE_EDGE(state) == 0U) {
 181   2              return MRT_EVENT_NONE;
 182   2          }
 183   1      
 184   1          decoder_state = MRT_DECODER_STATE(state);
 185   1      
 186   1          /* PAYLOAD 状态：等待 trailer */
 187   1          if (decoder_state == MRT_DECODER_PAYLOAD) {
 188   2              elapsed = MRT_EDGE_ELAPSED(now_ms, mrt->last_edge_ms);
 189   2              if (elapsed >= MRT_TRAILER_MIN_MS) {
 190   3                  payload_count = MRT_PAYLOAD_COUNT(state);
 191   3      
 192   3                  /* 3 个脉冲 = ENTER/RENEW */
 193   3                  if (payload_count == 3U) {
 194   4                      MRT_SET_MAINTENANCE_ACTIVE(state, 1U);
 195   4                      mrt->state = state;
 196   4                      mrt->lease_end_ms = MRT_LEASE_DEADLINE(now_ms);
 197   4                      mrt_decoder_reset(mrt);
 198   4                      return MRT_EVENT_ENTER;
 199   4                  }
 200   3      
 201   3                  /* 4 个脉冲 = EXIT */
 202   3                  if (payload_count == 4U) {
 203   4                      if (maintenance_active != 0U) {
 204   5                          MRT_SET_MAINTENANCE_ACTIVE(state, 0U);
 205   5                          mrt->state = state;
 206   5                          mrt->lease_end_ms = 0UL;
 207   5                      }
 208   4                      mrt_decoder_reset(mrt);
 209   4                      return MRT_EVENT_EXIT;
 210   4                  }
 211   3      
 212   3                  /* 其他脉冲数无效 */
 213   3                  mrt_decoder_reset(mrt);
 214   3              }
 215   2          }
 216   1          /* SYNC/DELIMITER 超时检查 */
 217   1          else if (decoder_state != MRT_DECODER_IDLE) {
 218   2              if (MRT_EDGE_ELAPSED(now_ms, mrt->last_edge_ms) >= MRT_SYNC_TIMEOUT_MS) {
 219   3                  mrt_decoder_reset(mrt);
 220   3              }
 221   2          }
 222   1      
 223   1          return MRT_EVENT_NONE;
 224   1      }
 225          
 226          /* 查询当前模式 */
 227          maintenance_runtime_mode_t maintenance_runtime_mode(
 228              const maintenance_runtime_t *mrt)
 229          {
 230   1          if (mrt == 0) return MRT_MODE_NORMAL;
 231   1          return MRT_MAINTENANCE_ACTIVE(mrt->state) != 0U
 232   1              ? MRT_MODE_MAINTENANCE
 233   1              : MRT_MODE_NORMAL;
 234   1      }
 235          
 236          #ifdef MAINTENANCE_RUNTIME_HOST_TEST
              /* 查询是否在维护模式 */
              unsigned char maintenance_runtime_is_active(const maintenance_runtime_t *mrt)
              {
C51 COMPILER V9.54   MAINTENANCE_RUNTIME                                                   08/21/2026 10:55:33 PAGE 5   

                  if (mrt == 0) return 0U;
                  return MRT_MAINTENANCE_ACTIVE(mrt->state);
              }
              
              /* 查询剩余租约时间（仅用于测试）*/
              maintenance_runtime_ms_t maintenance_runtime_remaining_ms(
                  const maintenance_runtime_t *mrt,
                  maintenance_runtime_ms_t now_ms)
              {
                  if (mrt == 0 || MRT_MAINTENANCE_ACTIVE(mrt->state) == 0U) {
                      return 0UL;
                  }
                  if (MRT_LEASE_EXPIRED(mrt, now_ms) != 0U) {
                      return 0UL;
                  }
                  return mrt->lease_end_ms - now_ms;
              }
              #endif


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =    946    ----
   CONSTANT SIZE    =   ----    ----
   XDATA SIZE       =   ----    ----
   PDATA SIZE       =   ----    ----
   DATA SIZE        =   ----      27
   IDATA SIZE       =   ----    ----
   BIT SIZE         =   ----    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
