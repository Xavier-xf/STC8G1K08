# v1.0.4 集成完成总结

## 完成时间
2026-08-20

## 核心成果

成功将 `maintenance_runtime` 模块集成到 STC8G1K08 固件中，实现了 P5.4 维护协议，同时保持 v1.0.3 的核心设计不变。

## 关键指标

### 内存占用（DATA）
```
- g_millisecond:              4 字节
- g_heartbeat_monitor:       13 字节
- g_reset_controller:         8 字节
- g_maintenance_runtime:      9 字节  ← 新增
- g_reset_output_active:      1 字节
- 局部变量估算:             ~20 字节
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  总计:                      ~55 字节 / 256 字节 (21.5%)
```

**对比原设计：**
- 原 decoder + controller: ~25 字节
- 新 maintenance_runtime: 9 字节
- **节省 16 字节 DATA**

### 功能完整性
✅ P5.4 协议解码（BREAK/SYNC/DELIMITER/PAYLOAD）
✅ ENTER 命令（3 个载荷脉冲）
✅ EXIT 命令（4 个载荷脉冲）
✅ 租约管理（默认 30 分钟，自动到期）
✅ 维护模式下 AP_RESET 高阻
✅ 退出/到期后恢复正常监控

### 设计原则遵守
✅ INT2 边沿先记入心跳，再交给解码器
✅ 协议解析不影响 AP_RESET 决策
✅ 全部状态保留在 DATA，无 XDATA 依赖
✅ 单次原子快照（v1.0.3 模式）
✅ 心跳监控路径完全不变

## 文件清单

### 新增文件
```
Driver/inc/maintenance_runtime.h           - 头文件（9B 结构体定义）
Driver/src/maintenance_runtime.c           - 实现（~200 行）
maintenance_runtime_test.c                 - 测试套件（8 个测试）
run_maintenance_runtime_test.sh            - 测试脚本
maintenance_runtime_integration_example.c  - 集成示例
MAINTENANCE_RUNTIME_SUMMARY.md             - 实现总结
```

### 修改文件
```
User/main.c                                - 集成维护事件处理
User/Config.h                              - 版本号 1.0.3 → 1.0.4
RVMDK/STC8G1K08-RESET.uvproj              - 添加 maintenance_runtime.c
reset_firmware_static_test.sh              - 更新静态检查规则
task_plan.md                               - 更新任务状态
progress.md                                - 记录进展
findings.md                                - 记录设计决策
```

## 测试验证

### 主机测试（全部通过）
```
✅ run_maintenance_runtime_test.sh
   - test_init
   - test_enter_command
   - test_exit_command
   - test_lease_expiry
   - test_normal_heartbeat_ignored
   - test_break_interrupted
   - test_renew_extends_lease
   - test_time_overflow

✅ run_heartbeat_monitor_test.sh
✅ run_reset_controller_test.sh
✅ reset_firmware_static_test.sh
```

### 静态检查要点
- 版本号正确（1.0.4）
- INT2 ISR 先心跳后解码器
- 维护事件处理完整
- 无 XDATA 依赖
- 无文本命令解析残留
- Keil 项目正确引用

## 代码结构

### INT2 中断服务程序
```c
void int2_isr(void) interrupt 10
{
    heartbeat_monitor_ms_t now_ms = g_millisecond;
    
    // 1. 先更新心跳（AP_RESET 决策关键）
    heartbeat_monitor_on_falling_edge(&g_heartbeat_monitor, now_ms);
    
    // 2. 再交给维护解码器（不影响心跳）
    maintenance_runtime_on_falling_edge(&g_maintenance_runtime, now_ms);
}
```

### 主循环逻辑
```c
while (1) {
    wdt_clear();
    heartbeat_snapshot(&heartbeat, &now_ms);
    
    // 轮询维护事件
    mrt_event = maintenance_runtime_poll(&g_maintenance_runtime, now_ms);
    mrt_mode = maintenance_runtime_mode(&g_maintenance_runtime);
    
    // 处理 ENTER/EXIT/EXPIRED
    if (mrt_event == MRT_EVENT_EXIT || mrt_event == MRT_EVENT_EXPIRED) {
        reset_controller_init(&g_reset_controller, now_ms);
    }
    
    // 维护模式：AP_RESET 高阻，跳过复位逻辑
    if (mrt_mode == MRT_MODE_MAINTENANCE) {
        if (g_reset_output_active) {
            apply_reset_output(0U);
        }
        continue;
    }
    
    // 正常模式：执行 v1.0.3 复位逻辑
    // ...
}
```

## 待完成工作（Phase 4）

### Windows Keil 编译
1. 打开 `RVMDK/STC8G1K08-RESET.uvproj`
2. 执行 `Rebuild` 操作
3. 验证编译结果：
   - `0 Error(s), 0 Warning(s)`
   - 检查 DATA 使用量（应 <100B）
   - 确认生成 `stc8g1k08_reset.hex`

### 板上验证
1. **P5.4 ENTER 命令**：V853 发送 3 个载荷脉冲帧
   - 验证 STC 进入维护模式
   - 确认 AP_RESET 保持高阻
   - 日志输出 "MNT ENTER"

2. **P5.4 EXIT 命令**：发送 4 个载荷脉冲帧
   - 验证 STC 退出维护模式
   - 确认恢复正常监控（30 秒启动宽限）
   - 日志输出 "MNT EXIT"

3. **租约到期**：进入维护模式后等待 30 分钟
   - 验证自动退出维护模式
   - 日志输出 "MNT EXPIRED"
   - 确认恢复正常监控

4. **掉电/复位恢复**：
   - 维护模式下给 STC 断电重启
   - 验证上电后进入正常监控模式（RAM-only 租约已丢失）
   - 验证心跳监控和 AP_RESET 功能正常

5. **心跳超时测试**：
   - 正常模式下停止 V853 心跳
   - 验证 5 秒后 AP_RESET 断言
   - 验证 200ms 后释放
   - 验证 V853 重启后恢复监控

## V853 控制工具

需要配套的 V853 工具支持：

```bash
# 进入维护模式（应用运行时通过 socket）
cpu_check_ctl enter

# 续租（应用停止后直接操作 GPIO）
cpu_check_ctl renew

# 退出维护模式
cpu_check_ctl exit
```

参考：`kocom-notification-blocker.md` 中的 V853 实现

## 关键约束重申

1. **INT2 边沿顺序强制**：必须先 `heartbeat_monitor_on_falling_edge()`，后 `maintenance_runtime_on_falling_edge()`
2. **DATA 限制**：全部 AP_RESET 决策数据必须在 DATA 中
3. **维护模式行为**：AP_RESET 必须高阻，不执行复位逻辑
4. **租约管理**：RAM-only，掉电/复位后自动恢复正常
5. **WDT 服务**：维护模式和正常模式都必须喂狗

## 与 v1.0.3 的差异

| 特性 | v1.0.3 | v1.0.4 |
|------|--------|--------|
| 版本号 | 1.0.3 | 1.0.4 |
| P5.4 维护协议 | 无 | 有（ENTER/EXIT/EXPIRED）|
| INT2 处理 | 仅心跳 | 心跳 + 解码器 |
| DATA 使用 | ~46B | ~55B (+9B) |
| 主循环 | 仅复位逻辑 | 复位 + 维护事件 |
| AP_RESET 控制 | 始终可用 | 维护模式禁用 |
| 心跳监控路径 | 不变 | 不变 |
| 复位决策快照 | 单次原子 | 单次原子（不变）|

## 成功标准

✅ **内存效率**：DATA <100B，节省 16B
✅ **功能完整**：P5.4 协议解码和维护模式管理
✅ **安全性**：心跳优先，协议不影响 AP_RESET 决策
✅ **可维护性**：单一模块，清晰接口
✅ **测试覆盖**：8 个主机测试全部通过
✅ **静态检查**：所有规则通过
✅ **兼容性**：保持 v1.0.3 核心不变

待完成：
⏳ Windows Keil 编译验证
⏳ 板上功能验证
